{% extends 'public/layout.html.twig' %}

{% block title %}Vous inscrire !{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.Nom) }}
        {{ form_row(registrationForm.Prenom) }}

        {{ form_row(registrationForm.Civilite) }}
        {{ form_row(registrationForm.dateNaissance) }}
        {{ form_row(registrationForm.noTelephone) }}
        {{ form_row(registrationForm.Ville) }}
        {{ form_row(registrationForm.codePostal) }}
        {{ form_row(registrationForm.Pays) }}
        {{ form_row(registrationForm.noSecu) }}
        
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Vous inscrire</button>
    {{ form_end(registrationForm) }}
{% endblock %}
